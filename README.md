# bliz_totp
This script helps generate a Time-based One-Time Password (TOTP) URL for Battle.net, streamlining the authentication process by converting device secrets into a format compatible with Battle.net's two-factor authentication system.
